# 롱 매수 전략 레퍼런스 v4.0 — 한국 시장 적용 + 코드 구현 완전판

## 45개 전략 × 상세 알고리즘 × 코드 구현 사양 × 6인 퀀트 검토 완료

---

# 변경 이력

| 버전 | 날짜 | 내용 |
|------|------|------|
| v1.0 | 2026-03-25 | 최초 작성: 미국 기준 50개 전략 |
| v2.0 | 2026-03-25 | Holly 전체 62개 전략 공식 가이드 기반 추가 |
| v3.0 | 2026-03-25 | 한국 시장 적용: 6인 퀀트 라운드테이블 검토, 45개 생존, 상세 알고리즘 조건 작성 |
| **v4.0** | **2026-03-25** | **코드 구현 사양 추가: 7가지 구조적 수정, 실행타이밍 태그, 통일 시그널/청산 규칙, 일봉 대체 조건, config 분리, Phase 구현 순서, 전략별 품질등급** |

---

# Section 0: v3.0 → v4.0 수정 사항 요약

## 수정 1: 고가 기준 명시
```
브레이크아웃 전략의 "N일 고가":
  → 장중 고가(고가 컬럼) 기준: MAX(df['high'], 최근 N일)
이동평균 비교:
  → 종가 기준: df['close'].rolling(N).mean()
```

## 수정 2: 일봉 대체 조건 추가
```
모든 "돌파 후 N분간 유지" 조건:
  [분봉 있을 때] 돌파 후 3분간 돌파 가격 위에서 유지
  [일봉만 있을 때] 당일 종가 > 돌파 가격 AND (종가-저가)/(고가-저가) >= 0.7
```

## 수정 3: 5분 거래량 대체
```
모든 "5분 거래량 >= N배" 조건:
  [분봉 있을 때] 5분_거래량 >= 20일_평균_5분_거래량 × N
  [일봉만 있을 때] 당일_거래량 >= 20일_평균_거래량 × N
```

## 수정 4: 실행 타이밍 태그
```
[EOD] 장 마감 후 일봉 스캔 → 다음날 시가 매수 (13개)
[INTRADAY] 장중 실시간 판단 필요, 분봉 필수 (12개)
[HYBRID] EOD 스캔으로 후보 → 장중 트리거 확인 (20개)

Phase 1: EOD 13개 (일봉만으로 완전 구현)
Phase 2: HYBRID 20개 (EOD 부분 먼저)
Phase 3: INTRADAY 12개 (분봉 확보 후)
```

## 수정 5: 통일 시그널 출력 형식
```python
signal = {
    "strategy_name": str,       # 전략 식별자 (snake_case)
    "ticker": str,              # 종목코드 "005930"
    "ticker_name": str,         # 종목명 "삼성전자"
    "direction": "LONG",        # 항상 LONG (롱 전략만)
    "entry_price": float,       # 진입가 (원)
    "target_price": float,      # 목표가 (원)
    "stop_loss_price": float,   # 손절가 (원)
    "target_pct": float,        # 목표 수익률 (0.03 = 3%)
    "stop_loss_pct": float,     # 손절 수익률 (-0.02 = -2%)
    "rr_ratio": float,          # Risk:Reward 비율
    "hold_days_min": int,       # 최소 보유일
    "hold_days_max": int,       # 최대 보유일
    "confidence": float,        # 신뢰도 0~1 (가중치 반영 후)
    "exec_timing": str,         # "EOD" | "INTRADAY" | "HYBRID"
    "category": str,            # "breakout" | "pullback" | "gap_momentum" | "support_bounce" | "mean_reversion" | "neo" | "legendary"
    "grade": str,               # "A" | "A-" | "B+" | "B" | "B-"
    "signal_date": str,         # "2026-03-25"
    "sector": str,              # WICS 소분류명
    "supply_demand_grade": str, # 수급등급 "A" | "B" | "C"
}
```

## 수정 6: 중복 시그널 처리 규칙
```
1. 동일 종목 다중 시그널 → confidence 가장 높은 것 1개 채택
   단, 3개 이상 전략이 동시에 가리키면 confidence × 1.3 부스팅
2. 동일 종목 최대 포지션: 총 자산의 10%
3. 동일 WICS 소분류 최대 포지션: 총 자산의 25%
4. 전체 포지션 상한: 총 자산의 80% (최소 20% 현금)
5. 하루 최대 신규 진입: 3종목
```

## 수정 7: 통일 청산 규칙 (우선순위 순)
```
1순위: 손절가 도달 → 즉시 청산 (무조건)
2순위: 목표가 도달 → 50% 익절, 나머지 트레일링 스탑
3순위: 최대 보유일 초과 → 다음날 시가 청산
4순위: 전략 조건 이탈 → 다음날 시가 청산

트레일링 스탑 (목표가 도달 후):
  trail_stop = MAX(종가, 보유 기간 중) × 0.95
  매일 장 마감 후 업데이트
```

---

# Section 1: 공통 설정

## 유니버스 필터
```yaml
# config/universe.yaml
min_market_cap: 200000000000    # 2,000억 원 (백테스트로 2,000~3,000억 탐색)
min_trading_value: 5000000000   # 50억 원 (최근 20일 평균)
min_price: 5000                 # 5,000원
max_suspension_days: 5          # 최근 252일 중 거래정지 5일 이상 제외
sector_source: "wiseindex"      # WICS 소분류
exclude_sectors: ["생물공학", "제약"]  # 선택적 제외
```

## 테마주/작전주 자동 제외 필터
```yaml
# config/universe.yaml (continued)
theme_filter:
  trading_value_to_mcap_ratio: 0.05    # 5일 평균 거래대금/시총 > 5% → 제외
  max_5d_return_without_earnings: 0.50  # 5일 수익률 > 50% + 실적 개선 없음 → 제외
  sector_simultaneous_surge_count: 5    # 동일 소분류 5개+ 동시 5%+ 급등 → 테마 경보
  sector_surge_confidence_discount: 0.5 # 테마 경보 시 개별 시그널 신뢰도 × 0.5
```

## 시장 환경 필터
```yaml
# config/market_filter.yaml
market_index:
  kospi_ticker: "KOSPI"
  kosdaq_ticker: "KOSDAQ"
  rule: "종목의 시장에 해당하는 지수 사용"

fx_filter:
  pair: "USD/KRW"
  ma_period: 5
  weakening_discount: 0.15  # 원화 약세 추세 시 신뢰도 × (1 - 0.15)
```

## 수급 필터
```yaml
# config/supply_demand.yaml
lookback_days: 5
grade_A: "외국인 순매수 + 기관 순매수"    # confidence × 1.2
grade_B: "한쪽만 순매수"                   # confidence × 1.0
grade_C: "둘 다 순매도"                    # confidence × 0.7 또는 제외
```

## 시간대별 신뢰도 가중치
```yaml
# config/market_filter.yaml (continued)
time_confidence:
  "09:00-09:30": 1.2
  "09:30-11:30": 1.0
  "11:30-13:00": 0.5   # 점심, 제외 권장
  "13:00-14:30": 1.0
  "14:30-15:20": 1.1
```

## 이벤트 필터
```yaml
# config/market_filter.yaml (continued)
earnings_season:
  buffer_before: 3      # 실적 발표 D-3
  buffer_after: 5       # 실적 발표 D+5
  confidence_discount: 0.5  # 백테스트로 0.3~0.7 사이 결정

special_dates:
  year_end: ["12-20", "01-05"]         # 배당락 기간
  quarterly_expiry_buffer_days: 1       # 분기 만기일 ±1일
  confidence_discount: 0.7
```

## 거래비용
```yaml
# config/trading_cost.yaml
buy_commission: 0.00015     # 0.015%
sell_commission: 0.00015    # 0.015%
securities_tax: 0.0018      # 0.18% (매년 확인 필요)
round_trip_cost: 0.0021     # 자동 계산: 위 3개 합산
min_expected_return: 0.0063 # round_trip_cost × 3
```

## RS Rating 계산
```yaml
# config/universe.yaml (continued)
rs_rating:
  weights:
    3month: 0.4
    6month: 0.2
    9month: 0.2
    12month: 0.2
  benchmark: "각 종목이 속한 시장 지수 (KOSPI or KOSDAQ)"
```

---

# Section 2: 전략 상세 — Phase 1 (EOD, 즉시 구현 가능, 12개)

> Phase 1 전략은 전부 **일봉 데이터만으로 완전 구현 가능**.
> 장 마감 후 스캔 → 다음날 시가 매수.

---

## 전략 1: Pushing The Spring [등급 A] [EOD]

> **한줄 설명**: 스프링을 누르고 있다가 놓으면 튕기듯, 좁은 횡보 후 돌파하는 종목을 잡는다.
>
> **원리**: 주가가 며칠간 좁은 범위에서 횡보하면 = 매수·매도 세력이 팽팽하게 균형을 이루는 상태. 이때 에너지가 축적되고, 어느 한쪽이 이기면(돌파) 축적된 에너지가 한 방향으로 폭발한다. 거래량 급증이 동반되면 진짜 돌파일 확률이 높다.
>
> **출처**: Darvas Box의 현대화. Trade Ideas Holly AI 실전 전략.
>
> **기대 시나리오**: 3~5일 타이트 횡보 → 거래량 2배 동반 돌파 → 1~5일간 3%+ 상승

```yaml
# config/strategies/breakout.yaml
pushing_the_spring:
  category: "breakout"
  exec_timing: "EOD"
  grade: "A"

  universe:
    min_price: 5000
    min_trading_value: 5000000000

  scan_conditions:
    # 사전 조건: 전일 장 마감 후 스캔
    range_squeeze:
      desc: "최근 5일 레인지가 10일 레인지의 60% 미만"
      formula: |
        range_5d = (df['high'].rolling(5).max() - df['low'].rolling(5).min()) / df['close'].rolling(5).mean()
        range_10d = (df['high'].rolling(10).max() - df['low'].rolling(10).min()) / df['close'].rolling(10).mean()
        condition = range_5d < range_10d * 0.6

    small_body:
      desc: "최근 3일 캔들 몸통이 20일 평균의 50% 미만"
      formula: |
        body = (df['close'] - df['open']).abs()
        avg_body_20d = body.rolling(20).mean()
        condition = body.rolling(3).mean() < avg_body_20d * 0.5

  trigger_conditions:
    breakout:
      desc: "당일 종가가 최근 5일 장중 고가를 돌파"
      formula: |
        resistance = df['high'].rolling(5).max().shift(1)
        condition = df['close'] > resistance

    volume_confirm:
      desc: "당일 거래량이 20일 평균의 2배 이상"
      formula: |
        condition = df['volume'] > df['volume'].rolling(20).mean() * 2.0

    close_strength:
      desc: "종가가 당일 레인지 상위 30%"
      formula: |
        day_range = df['high'] - df['low']
        position = (df['close'] - df['low']) / day_range
        condition = position >= 0.7

  signal_output:
    entry: "다음날 시가"
    target_pct: 0.03        # +3%에서 시작, 백테스트로 조정
    stop_loss_formula: "MAX(5일_저가, entry × 0.97)"
    hold_days_min: 1
    hold_days_max: 5
    exit_condition: "5일 이평 하회 시"
```

---

## 전략 2: Engulfing [등급 A] [EOD]

> **한줄 설명**: 전일 음봉을 당일 양봉이 완전히 삼키면, 추세 전환 시작.
>
> **원리**: 전일에 매도세가 이겼지만(음봉), 당일에 매수세가 전일의 전체 범위를 뒤덮었다(양봉 장악). 이건 "매도세가 완전히 제압당했다"는 신호. 거래량이 전일보다 많으면 확신이 더 강하다.
>
> **출처**: 일본 캔들스틱 차트의 대표 반전 패턴. 수백 년 역사.
>
> **기대 시나리오**: 하락 중 양봉 장악형 출현 → 1~3일간 3~5% 반등

```yaml
engulfing:
  category: "breakout"
  exec_timing: "EOD"
  grade: "A"

  universe:
    min_price: 5000
    min_trading_value: 5000000000

  scan_conditions:
    prev_day_bearish:
      desc: "전일이 음봉"
      formula: |
        condition = df['close'].shift(1) < df['open'].shift(1)

    engulfing_pattern:
      desc: "당일 레인지가 전일 레인지를 완전히 감싸고 양봉"
      formula: |
        condition = (
            (df['low'] < df['low'].shift(1)) &      # 전일 저가보다 낮고
            (df['close'] > df['high'].shift(1)) &    # 전일 고가보다 높게 마감
            (df['close'] > df['open'])                # 양봉
        )

    range_expansion:
      desc: "당일 레인지가 전일의 120% 이상"
      formula: |
        today_range = df['high'] - df['low']
        yesterday_range = df['high'].shift(1) - df['low'].shift(1)
        condition = today_range > yesterday_range * 1.2

    volume_confirm:
      desc: "거래량이 전일의 1.5배 이상"
      formula: |
        condition = df['volume'] > df['volume'].shift(1) * 1.5

  signal_output:
    entry: "다음날 시가"
    target_pct: 0.04
    stop_loss_formula: "당일_저가 또는 entry × 0.97 중 높은 것"
    hold_days_min: 1
    hold_days_max: 3
```

---

## 전략 3: Yesterday Hammer [등급 A] [EOD]

> **한줄 설명**: 전일 해머 캔들(장중 급락 후 강한 반등 마감) → 다음날 강세 확인되면 매수.
>
> **원리**: 해머 = 장중에 매도세가 가격을 크게 밀어내렸지만, 매수세가 되받아쳐서 거의 고가 근처로 마감. "매도세가 총알을 다 썼다"는 뜻. 다음날 강세가 이어지면 전환이 확인된 것.
>
> **출처**: 일본 캔들스틱 차트 + Trade Ideas Holly AI.
>
> **기대 시나리오**: 전일 해머(하꼬리 길고 상꼬리 거의 없음) → 당일 갭업 + 전일 고가 돌파 → 1~3일간 3~5% 상승

```yaml
yesterday_hammer:
  category: "support_bounce"
  exec_timing: "EOD"
  grade: "A"

  universe:
    min_price: 5000
    min_trading_value: 5000000000

  scan_conditions:
    hammer_candle:
      desc: "전일 해머 캔들: 하단꼬리 >= 몸통×2, 상단꼬리 <= 몸통×0.3"
      formula: |
        body = (df['close'].shift(1) - df['open'].shift(1)).abs()
        lower_wick = df[['close','open']].shift(1).min(axis=1) - df['low'].shift(1)
        upper_wick = df['high'].shift(1) - df[['close','open']].shift(1).max(axis=1)
        condition = (lower_wick >= body * 2) & (upper_wick <= body * 0.3) & (body > 0)

    prev_close_position:
      desc: "전일 종가가 전일 레인지 상위 30%"
      formula: |
        prev_range = df['high'].shift(1) - df['low'].shift(1)
        prev_position = (df['close'].shift(1) - df['low'].shift(1)) / prev_range
        condition = prev_position >= 0.7

    today_strength:
      desc: "당일 시가 > 전일 종가 AND 당일 종가 > 전일 고가"
      formula: |
        condition = (df['open'] > df['close'].shift(1)) & (df['close'] > df['high'].shift(1))

    volume_confirm:
      desc: "당일 거래량 >= 전일"
      formula: |
        condition = df['volume'] >= df['volume'].shift(1)

  signal_output:
    entry: "다음날 시가"
    target_pct: 0.04
    stop_loss_formula: "전일_저가(해머 꼬리 끝) 또는 entry × 0.97"
    hold_days_min: 1
    hold_days_max: 3
```

---

## 전략 4: Snap Back Long [등급 A] [EOD]

> **한줄 설명**: 5일 연속 하락 + RSI 과매도 → 첫 양봉 나오면 반등에 탑승.
>
> **원리**: 고무줄을 너무 많이 당기면 결국 튕겨 돌아온다. 장기 상승 추세(60일 이평 상승)가 살아있는 종목이 단기적으로 과매도(5일 연속 하락, RSI<30)되면, 평균으로 회귀할 확률이 높다. 첫 양봉 = 매도세 소진 + 매수세 유입의 첫 신호.
>
> **출처**: 평균 회귀 이론 + 르네상스 Statistical Arbitrage 원리의 개별 종목 적용.
>
> **기대 시나리오**: 5일 하락(-8%+) + RSI 30 미만 → 첫 양봉 → 3~7일간 5% 반등

```yaml
snap_back_long:
  category: "mean_reversion"
  exec_timing: "EOD"
  grade: "A"

  universe:
    min_price: 10000
    min_trading_value: 5000000000

  scan_conditions:
    consecutive_decline:
      desc: "최근 5일 중 3일 이상 음봉"
      formula: |
        bearish_days = (df['close'] < df['close'].shift(1)).rolling(5).sum()
        condition = bearish_days >= 3

    significant_drop:
      desc: "5일 누적 수익률 < -8%"
      formula: |
        return_5d = df['close'] / df['close'].shift(5) - 1
        condition = return_5d < -0.08

    oversold_rsi:
      desc: "RSI(14) < 30"
      formula: |
        # RSI 계산
        delta = df['close'].diff()
        gain = delta.where(delta > 0, 0).rolling(14).mean()
        loss = (-delta.where(delta < 0, 0)).rolling(14).mean()
        rs = gain / loss
        rsi = 100 - (100 / (1 + rs))
        condition = rsi < 30

    bounce_start:
      desc: "당일 양봉 (종가 > 전일 종가) + 거래량 증가"
      formula: |
        condition = (
            (df['close'] > df['close'].shift(1)) &
            (df['volume'] > df['volume'].rolling(5).mean())
        )

    long_term_trend_intact:
      desc: "60일 이평이 여전히 상승 중 (장기 추세 안에서의 과매도)"
      formula: |
        ma60 = df['close'].rolling(60).mean()
        condition = ma60 > ma60.shift(5)

  signal_output:
    entry: "다음날 시가"
    target_pct: 0.05
    stop_loss_formula: "5일 연속하락 저점 또는 entry × 0.96"
    hold_days_min: 3
    hold_days_max: 7
```

---

## 전략 5: Horseshoe Up [등급 A] [EOD]

> **한줄 설명**: 갭업 → 하락(갭 메꿈 시도) → 다시 시가 위로 복귀 = U자 반등, 매수.
>
> **원리**: 갭업 후 이익실현 매물이 쏟아지면서 갭을 메꾸려 한다. 근데 매도세가 갭을 다 메꾸지 못하고 다시 올라왔다면? 그건 매수세가 매도세보다 강하다는 증거. "갭 메꿈 실패 = 강한 매수 신호". 말발굽(U자) 모양.
>
> **출처**: Trade Ideas Holly AI 실전 전략.
>
> **기대 시나리오**: 갭업 1.5%+ → 시가 아래로 1%+ 하락 → 다시 시가 위 마감 → 1~3일간 3% 추가 상승

```yaml
horseshoe_up:
  category: "support_bounce"
  exec_timing: "EOD"
  grade: "A"

  universe:
    min_price: 5000
    min_trading_value: 5000000000

  scan_conditions:
    gap_up:
      desc: "당일 갭업 1.5% 이상"
      formula: |
        gap = (df['open'] - df['close'].shift(1)) / df['close'].shift(1)
        condition = gap >= 0.015

    gap_fill_attempt:
      desc: "장중 시가 아래로 1% 이상 하락 (갭 메꿈 시도)"
      formula: |
        condition = df['low'] < df['open'] * 0.99

    recovery:
      desc: "종가가 시가 위로 복귀 (갭 메꿈 실패 = 강세)"
      formula: |
        condition = df['close'] > df['open']

    close_strength:
      desc: "종가가 당일 레인지 상위 40%"
      formula: |
        position = (df['close'] - df['low']) / (df['high'] - df['low'])
        condition = position >= 0.6

  signal_output:
    entry: "다음날 시가"
    target_pct: 0.03
    stop_loss_formula: "당일_저가 또는 entry × 0.98"
    hold_days_min: 1
    hold_days_max: 3
```

---

## 전략 6: Volume Doesn't Lie [등급 A] [EOD]

> **한줄 설명**: 4%+ 갭업 + 거래량 2배 폭발 = "거래량은 거짓말을 안 한다", 방향 지속에 베팅.
>
> **원리**: 큰 갭 + 대량 거래량 = 뉴스·실적·기관 매수 등 강력한 촉매가 있다는 증거. 거래량은 "확신의 크기"를 보여줌. 갭업 후 종가가 시가 위에서 유지되면 갭 방향 지속 확률이 높다.
>
> **출처**: Trade Ideas Holly AI. 원래 멀티데이 보유용이었으나 인트라데이용으로 재설계됨. 한국 적용 시 스윙(1~5일)으로 운용.
>
> **기대 시나리오**: 갭업 4%+ 오픈 + 거래량 2배 → 당일 시가 위 마감 → 1~5일간 6%+ 추가 상승

```yaml
volume_doesnt_lie:
  category: "gap_momentum"
  exec_timing: "EOD"
  grade: "A"

  universe:
    min_price: 5000
    min_trading_value: 5000000000

  scan_conditions:
    gap_up:
      desc: "당일 갭업 4% 이상 (백테스트로 3~4% 탐색)"
      formula: |
        gap = (df['open'] - df['close'].shift(1)) / df['close'].shift(1)
        condition = gap >= 0.04  # config에서 조정 가능

    volume_surge:
      desc: "당일 거래량이 20일 평균의 2배 이상"
      formula: |
        condition = df['volume'] > df['volume'].rolling(20).mean() * 2.0

    gap_hold:
      desc: "종가가 시가 이상 (갭 메꿈 안 됨)"
      formula: |
        condition = df['close'] >= df['open']

    close_strength:
      desc: "종가가 당일 레인지 상위 50%"
      formula: |
        position = (df['close'] - df['low']) / (df['high'] - df['low'])
        condition = position >= 0.5

  theme_filter:
    desc: "테마주 필터 — 같은 소분류 5개+ 동시 갭업 시 신뢰도 하향"
    formula: |
      sector_gap_count = 동일_소분류_종목_중_갭업_4%_이상_수
      if sector_gap_count >= 5:
          confidence *= 0.6  # 테마 갭 의심

  signal_output:
    entry: "다음날 시가"
    target_pct: 0.06
    stop_loss_formula: "당일_시가(갭업 시가) 하회 시 또는 entry × 0.95"
    hold_days_min: 1
    hold_days_max: 5
```

---

## 전략 7: Minervini Trend Template [등급 A] [EOD]

> **한줄 설명**: 미국 투자 챔피언십 우승자의 비법 — "Stage 2 상승 추세 종목만 산다".
>
> **원리**: Mark Minervini는 8가지 조건으로 "지금 상승 추세가 확실한 종목"만 골라낸다. 모든 이평(50/150/200일)이 정배열이고, 52주 최고가 근처이고, 상대강도가 상위 30%인 종목 = 기관이 매집 중인 상승 추세 종목. 여기에 거래량 폭발이 동반되면 진입.
>
> **출처**: Mark Minervini "Trade Like a Stock Market Wizard". SEPA(Specific Entry Point Analysis) 시스템. 연평균 220% 수익률.
>
> **기대 시나리오**: 8개 조건 전부 충족 + 당일 2%+ 상승 + 거래량 폭발 → 2주~3개월간 추세 추종

```yaml
minervini_trend_template:
  category: "legendary"
  exec_timing: "EOD"
  grade: "A"

  universe:
    min_price: 5000
    min_trading_value: 5000000000

  scan_conditions:
    above_150ma:
      formula: "df['close'] > df['close'].rolling(150).mean()"

    above_200ma:
      formula: "df['close'] > df['close'].rolling(200).mean()"

    ma150_above_ma200:
      formula: "df['close'].rolling(150).mean() > df['close'].rolling(200).mean()"

    ma200_rising:
      desc: "200일 이평이 22거래일(1개월) 동안 상승"
      formula: |
        ma200 = df['close'].rolling(200).mean()
        condition = ma200 > ma200.shift(22)

    above_50ma:
      formula: "df['close'] > df['close'].rolling(50).mean()"

    above_52w_low_30pct:
      desc: "52주 최저가 대비 30% 이상 위"
      formula: |
        low_52w = df['low'].rolling(252).min()
        condition = df['close'] > low_52w * 1.30

    within_52w_high_25pct:
      desc: "52주 최고가 대비 25% 이내 (백테스트로 20~25% 탐색)"
      formula: |
        high_52w = df['high'].rolling(252).max()
        condition = df['close'] > high_52w * 0.75

    rs_rating:
      desc: "RS Rating >= 70"
      formula: |
        # RS Rating은 별도 함수로 전체 유니버스 대비 계산
        condition = rs_rating >= 70

  trigger_conditions:
    desc: "위 8개 모두 충족 + 당일 2%+ 상승 + 거래량 2배"
    formula: |
      daily_return = df['close'] / df['close'].shift(1) - 1
      vol_surge = df['volume'] > df['volume'].rolling(20).mean() * 2.0
      trigger = (daily_return >= 0.02) & vol_surge & all_scan_conditions

  signal_output:
    entry: "다음날 시가"
    target_pct: null           # 추세 추종: 고정 목표 없음
    stop_loss_formula: "50일 이평 하회 시 또는 entry × 0.92 (8%)"
    hold_days_min: 10
    hold_days_max: 60
    exit_condition: "150일 이평 하회 OR RS_Rating < 50 OR 50일 이평 하향 전환"
```

---

## 전략 8: Darvas Box [등급 A] [EOD]

> **한줄 설명**: 주가가 상자(Box) 안에서 횡보하다가 상자 위로 탈출하면 매수.
>
> **원리**: Nicolas Darvas가 발견한 패턴 — 주가가 신고가를 찍고 며칠간 그 근처에서 횡보하면 "박스"가 형성된다. 박스 안 = 매수·매도 세력의 전투. 박스 상단 돌파 = 매수세 승리 → 다음 박스로 상승. 거래량 급증이 동반되면 진짜 돌파.
>
> **출처**: Nicolas Darvas "How I Made $2,000,000 in the Stock Market". 1960년대. 성혁이 기존 박스권 스크리너와 직접 연결.
>
> **기대 시나리오**: 박스 형성(3일+) → 상단 돌파 + 거래량 2배 → 박스 높이만큼 추가 상승 기대

```yaml
darvas_box:
  category: "legendary"
  exec_timing: "EOD"
  grade: "A"

  universe:
    min_price: 5000
    min_trading_value: 5000000000

  scan_conditions:
    box_top:
      desc: "신고가 후 3일간 해당 고가 미돌파 → 박스 상단 확정"
      formula: |
        new_high = df['high'] == df['high'].rolling(20).max()
        no_break_3d = 신고가 이후 3거래일 동안 해당 고가 미돌파
        box_top = 해당 고가

    box_bottom:
      desc: "박스 상단 확정 후 3일간 저점 미갱신 → 박스 하단 확정"
      formula: |
        box_bottom_period = 박스 상단 확정 후 3거래일
        no_lower_low = 해당 기간 저점이 더 내려가지 않음
        box_bottom = 해당 기간 최저가

    box_height_check:
      desc: "박스 높이가 박스 중간값의 15% 이내"
      formula: |
        box_height = box_top - box_bottom
        box_mid = (box_top + box_bottom) / 2
        condition = box_height / box_mid <= 0.15

  trigger_conditions:
    breakout:
      desc: "종가가 박스 상단 돌파 + 거래량 2배"
      formula: |
        condition = (
            (df['close'] > box_top * 1.005) &
            (df['volume'] > df['volume'].rolling(20).mean() * 2.0)
        )

    close_strength:
      desc: "[일봉 대체] 종가가 레인지 상위 30%"
      formula: |
        position = (df['close'] - df['low']) / (df['high'] - df['low'])
        condition = position >= 0.7

  signal_output:
    entry: "다음날 시가"
    target_formula: "entry + box_height (측정 목표치)"
    stop_loss_formula: "box_bottom 또는 entry × 0.95"
    hold_days_min: 1
    hold_days_max: 10
    pyramid: "새 박스 상단 돌파 시 추가 매수 가능"
```

---

## 전략 9: Weinstein Stage Analysis [등급 A] [EOD]

> **한줄 설명**: 주가는 4단계 사이클을 반복한다 — "Stage 2 상승기에만 매수, 나머진 쳐다보지도 마라."
>
> **원리**: Stan Weinstein은 모든 종목이 바닥 횡보(Stage 1) → 상승(Stage 2) → 천장 횡보(Stage 3) → 하락(Stage 4)을 반복한다고 봤다. 핵심은 150일(30주) 이동평균의 방향. 이평이 상승 전환하면서 주가가 이평 위로 올라오면 Stage 2 시작. 이때만 매수하면 큰 하락을 피할 수 있다.
>
> **출처**: Stan Weinstein "Secrets For Profiting in Bull and Bear Markets". 30년+ 검증.
>
> **기대 시나리오**: 150일 이평 수평→상승 전환 + 주가 이평 상향 돌파 + 거래량 급증 → 수주~수개월간 추세 추종

```yaml
weinstein_stage:
  category: "legendary"
  exec_timing: "EOD"
  grade: "A"

  universe:
    min_price: 5000
    min_trading_value: 5000000000

  scan_conditions:
    stage_classification:
      desc: "150일(30주) 이동평균 기준 4단계 판별"
      formula: |
        ma150 = df['close'].rolling(150).mean()
        ma150_slope = ma150 - ma150.shift(22)  # 1개월 기울기

        # Stage 1: 바닥 횡보
        stage1 = (ma150_slope.abs() / ma150 < 0.01) & (abs(df['close'] - ma150) / ma150 < 0.05)

        # Stage 2: 상승 추세 ★ 매수 가능
        stage2 = (df['close'] > ma150) & (ma150_slope > 0)

        # Stage 3: 천장 횡보
        stage3 = (ma150_slope.abs() / ma150 < 0.01) & (df['close'] > ma150 * 0.95)

        # Stage 4: 하락 추세
        stage4 = (df['close'] < ma150) & (ma150_slope < 0)

  trigger_conditions:
    stage2_entry:
      desc: "Stage 1 → Stage 2 전환 + 거래량 급증 + 5일 유지 확인"
      formula: |
        # 전환 감지: 5일 전 stage1이었고 현재 stage2
        transition = stage1.shift(5) & stage2

        # 전환 시점 거래량
        volume_surge = df['volume'] > df['volume'].rolling(20).mean() * 2.0

        # 5일간 150일 이평 위 유지
        sustained = (df['close'].rolling(5).min() > ma150)

        trigger = transition & volume_surge & sustained

  signal_output:
    entry: "5일 유지 확인 후 다음날 시가"
    target_pct: null           # 추세 추종
    stop_loss_formula: "150일 이평 하회 시"
    hold_days_min: 20
    hold_days_max: 120
    exit_condition: "Stage 3 전환 감지 시 (이평 기울기 감소 시작)"
```

---

## 전략 10: Greenblatt Magic Formula [등급 A] [EOD]

> **한줄 설명**: "좋은 회사를 싸게 사라" — 자본수익률(ROIC) 높고 이익수익률(EY) 높은 종목을 기계적으로 매수.
>
> **원리**: Joel Greenblatt의 핵심 아이디어 — 좋은 회사(ROIC 높음 = 투자한 돈 대비 이익을 많이 냄)가 싼 가격(EY 높음 = 시장가 대비 이익이 많음)에 거래되고 있으면, 결국 시장이 제값을 매겨줄 때 수익이 난다. 두 지표를 각각 순위 매겨서 합산하면 끝.
>
> **출처**: Joel Greenblatt "The Little Book That Beats the Market". Gotham Capital 연평균 33%(16년). 글로벌 다수 시장에서 백테스트 검증됨.
>
> **기대 시나리오**: 상위 20~30개 종목 동일 비중 매수 → 분기 리밸런싱 → 연간 KOSPI 대비 5~10% 초과수익

```yaml
magic_formula:
  category: "legendary"
  exec_timing: "EOD"
  grade: "A"

  universe:
    min_market_cap: 200000000000  # 2,000억
    min_trading_value: 3000000000  # 30억
    exclude_sectors: ["은행", "증권", "손해보험", "생명보험", "카드", "기타금융", "유틸리티"]
    financial_data: "연결 재무제표 기준"

  factor_calculation:
    roic:
      desc: "자본수익률"
      formula: |
        ebit = 영업이익  # DART 기준
        net_working_capital = 유동자산 - 유동부채
        net_fixed_assets = 총자산 - 유동자산 - 무형자산
        invested_capital = net_working_capital + net_fixed_assets
        roic = ebit / invested_capital  # 높을수록 좋음

    earnings_yield:
      desc: "이익수익률"
      formula: |
        ev = 시가총액 + 총차입금 - 현금성자산
        earnings_yield = ebit / ev  # 높을수록 싸고 좋음

  ranking:
    desc: "두 팩터 각각 순위 매기고 합산"
    formula: |
      roic_rank = 전체_유니버스.rank(by='roic', ascending=False)
      ey_rank = 전체_유니버스.rank(by='earnings_yield', ascending=False)
      combined_rank = roic_rank + ey_rank
      # combined_rank가 작을수록 좋음 (1위+1위=2가 최고)

  signal_output:
    entry: "combined_rank 상위 20~30개 종목 매수"
    allocation: "동일 비중 (1/N)"
    rebalance: "분기 1회 (한국 실적 발표 주기 맞춤)"
    hold_days_min: 60
    hold_days_max: 90
    note: "CB/BW 발행 기업 주의 표시, 최근 1년 대규모 유상증자 기업 제외"
```

---

## 전략 11: Piotroski F-Score [등급 A] [EOD]

> **한줄 설명**: 싸 보이는 주식 중에서 "진짜 싼 보석"과 "싸보이지만 쓰레기"를 9점 체크리스트로 구분.
>
> **원리**: 저PBR(싼) 주식을 사면 돈을 벌 수 있다고 알려져 있지만, 싼 데는 이유가 있는 경우가 많다. Piotroski 교수는 수익성(4점), 재무 안정성(3점), 효율성(2점) 총 9개 항목으로 재무 건전성을 점수화했다. 8~9점이면 진짜 저평가, 0~2점이면 부실 위험.
>
> **출처**: Joseph Piotroski, 시카고 대학 교수. "Value Investing: The Use of Historical Financial Statement Information to Separate Winners from Losers" (2000). 학술 논문으로 검증.
>
> **기대 시나리오**: 저PBR 종목 중 F-Score 8~9점 매수 → 분기 리밸런싱 → 시장 대비 초과수익

```yaml
piotroski_fscore:
  category: "legendary"
  exec_timing: "EOD"
  grade: "A"

  universe:
    filter: "PBR 하위 20% 종목 (저PBR 가치주)"
    min_market_cap: 200000000000
    financial_data: "연결 재무제표 기준"

  score_calculation:
    profitability:
      f1: "당기순이익 > 0 → +1"
      f2: "영업현금흐름 > 0 → +1"
      f3: "ROA(당기) > ROA(전기) → +1"
      f4: "영업현금흐름 > 당기순이익 → +1"

    financial_stability:
      f5: "장기부채비율(당기) < 장기부채비율(전기) → +1"
      f6: "유동비율(당기) > 유동비율(전기) → +1"
      f7: "신주 발행 없음 → +1 (유상증자 + CB/BW 전환 포함, 최근 1년)"

    efficiency:
      f8: "매출총이익률(당기) > 매출총이익률(전기) → +1"
      f9: "자산회전율(당기) > 자산회전율(전기) → +1 (자산회전율 = 매출/총자산)"

    formula: |
      fscore = f1 + f2 + f3 + f4 + f5 + f6 + f7 + f8 + f9  # 0~9점

  signal_output:
    entry: "F-Score 8~9점 종목 매수"
    allocation: "동일 비중"
    rebalance: "분기 1회"
    hold_days_min: 60
    hold_days_max: 90
    avoid: "F-Score 0~3점은 매수 회피 블랙리스트에 추가"
```

---

## 전략 12: Livermore Pivot Point [등급 A] [EOD]

> **한줄 설명**: "저항을 뚫는 순간이 가장 확률 높은 매수 시점" — 신고가 돌파 시 매수, 맞으면 추가(피라미딩).
>
> **원리**: Jesse Livermore의 핵심 — 주가가 오랫동안 넘지 못했던 저항선(Pivotal Point)을 뚫으면, 그 위에 매도 물량이 없어서 "최소 저항선(Path of Least Resistance)"을 따라 빠르게 올라간다. 한 번에 몰빵하지 않고, 맞으면 50%→25%→25%로 추가 매수(피라미딩)하고, 틀리면 빠르게 손절.
>
> **출처**: Jesse Livermore "Reminiscences of a Stock Operator". 20세기 초. 투기의 교과서.
>
> **기대 시나리오**: 52주 신고가 돌파 + 거래량 폭발 → 50% 진입 → 2~3% 추가 상승 시 25% 추가 → 추세 추종

```yaml
livermore_pivot:
  category: "legendary"
  exec_timing: "EOD"
  grade: "A"

  universe:
    min_price: 10000
    min_trading_value: 8000000000

  scan_conditions:
    pivotal_point:
      desc: "52주 신고가 돌파 OR 3개월 고가 돌파"
      formula: |
        high_52w = df['high'].rolling(252).max().shift(1)
        high_3m = df['high'].rolling(63).max().shift(1)
        condition = (df['close'] > high_52w) | (df['close'] > high_3m)

    volume_surge:
      desc: "돌파일 거래량 2배 이상"
      formula: |
        condition = df['volume'] > df['volume'].rolling(20).mean() * 2.0

    rs_filter:
      desc: "RS Rating >= 70"
      formula: "condition = rs_rating >= 70"

  signal_output:
    entry_pyramid:
      desc: "피라미딩 방식"
      step1: "Pivotal Point 돌파 시 목표 포지션의 50%. 다음날 시가."
      step2: "1차 매수 후 2~3% 추가 상승 시 25% 추가"
      step3: "2차 매수 후 2~3% 추가 상승 시 25% 추가"
    target_pct: null  # 추세 추종
    stop_loss:
      step1: "진입가 × 0.97 하회 → 전체 손절"
      step2: "2차 진입가 × 0.97 하회 → 2,3차 손절"
    max_risk_per_trade: "총 자산의 2%"
    hold_days_min: 5
    hold_days_max: 90
```

---

# Section 3: 전략 상세 — Phase 2 (HYBRID, EOD 부분 먼저 구현, 20개)

> 아래 전략들은 EOD 스캔으로 후보를 추리고,
> 장중에 트리거를 확인하는 구조.
> Phase 2에서는 **EOD 스캔 부분만 먼저 구현** (후보 리스트 생성).
> 장중 트리거는 분봉 데이터 확보 후 Phase 3에서 추가.

---

## 전략 13: Bullish Trend Change [등급 B+] [HYBRID]

> **한줄 설명**: 하락 추세에서 상승 추세로 전환되는 첫 순간을 잡는다 — 20일 이평 상향 돌파.
>
> **원리**: 주가가 20일 이평 아래에 있었다는 건 단기 하락 추세였다는 뜻. 이걸 뚫고 올라오면 "추세가 바뀌었다"는 첫 번째 신호. 60분 고가도 갱신하면 장중에도 강세가 확인된 것.
>
> **출처**: Trade Ideas Holly AI. 추세 전환 초기 포착 전략.
>
> **기대 시나리오**: 20일 이평 아래 → 거래량 2배 동반 상향 돌파 → 2~5일간 5% 상승

```yaml
bullish_trend_change:
  category: "breakout"
  exec_timing: "HYBRID"
  grade: "B+"

  eod_scan:
    desc: "전일까지 20일 이평 아래 → 당일 20일 이평 상향 돌파"
    formula: |
      ma20 = df['close'].rolling(20).mean()
      condition = (df['close'].shift(1) < ma20.shift(1)) & (df['close'] > ma20)

    volume: "df['volume'] > df['volume'].rolling(20).mean() * 2.0"
    min_price: 50000
    min_trading_value: 8000000000

  intraday_trigger:
    desc: "[분봉 있을 때] 60분 고가 갱신 + 3분 유지"
    eod_alternative: "당일 종가가 레인지 상위 30% + 종가 > 20일이평"

  signal_output:
    entry: "다음날 시가"
    target_pct: 0.05
    stop_loss_formula: "20일 이평 하회 시 또는 entry × 0.97"
    hold_days_min: 2
    hold_days_max: 5
```

---

## 전략 14~32: HYBRID 전략 상세 설명

> 아래 전략들은 v3.0의 알고리즘 조건을 그대로 유지.
> 일봉 대체 조건 적용: "돌파 3분 유지" → "종가>돌파가 + 레인지 상위 30%", "5분 거래량 2배" → "일일 거래량 2배"

---

### 전략 14: Float On [등급 B] [HYBRID]
> **한줄 설명**: 유통주식 적은 종목이 저항을 뚫을 때 — 공급이 적으면 작은 수요로도 가격이 크게 움직인다.
>
> **원리**: 유통주식 1000만주 미만 = 시장에 풀린 주식이 적다. 여기에 매수세가 몰리면 "품절 효과"로 가격이 급등. RS Rating 70+ 필터로 이미 강한 종목만 고르고, 시장 전체가 강세(지수 양봉)일 때만 진입해서 거짓 돌파를 필터링.
>
> **출처**: Trade Ideas Holly AI.
>
> **기대 시나리오**: 저유통주 + RS 강세 + 지수 양봉 → 저항 돌파 → 1~3일간 4~6% 상승

### 전략 15: Wake Up Call [등급 B] [HYBRID]
> **한줄 설명**: 저유통 종목이 신고가를 갱신할 때 — "잠자던 종목이 깨어난다."
>
> **원리**: Float On과 유사하지만, 이건 "신고가 갱신"이 트리거. 20일 고가를 돌파하면서 이평 정배열(5분>20분>60분)이 되어 있으면, 모든 타임프레임에서 상승 추세가 정렬된 상태. 최근 5분간 상승 중이어야 "지금 올라가고 있다"를 확인.
>
> **출처**: Trade Ideas Holly AI.
>
> **기대 시나리오**: 저유통 + 20일 신고가 + 이평 정배열 → 1~3일간 5~8% 상승

### 전략 16: Staggering Volume [등급 B] [HYBRID]
> **한줄 설명**: 거래량이 "어마어마하게(staggering)" 폭발하면서 신고가 → 뭔가 큰 일이 벌어지고 있다.
>
> **원리**: 상대거래량, 5분거래량, 1분거래량이 전부 평소의 3~5배 이상 = 기관이든 개인이든 엄청난 매수세가 몰리고 있다. 여기에 공매도 잔고(days to cover)가 높으면 숏스퀴즈까지 가능. 가장 폭발적이지만 가장 위험한 전략.
>
> **출처**: Trade Ideas Holly AI.
>
> **기대 시나리오**: 거래량 전 지표 폭발 + 신고가 → 당일~2일간 6~10% 급등. 테마주 필터 필수.

### 전략 17: Close to a Cross [등급 A-] [HYBRID]
> **한줄 설명**: 단기 이평이 50일 이평을 골든크로스하는 순간 — 기관들이 이 신호를 주시한다.
>
> **원리**: 많은 기관들이 50일 이평 교차를 매매 신호로 사용. 그래서 골든크로스가 발생하면 "교차를 보고 매수하는 기관 매수"가 추가로 유입되어 자기실현적 예언이 된다. 강한 장중 움직임(당일 2%+)이 동반되면 교차가 확인된 것.
>
> **출처**: Trade Ideas Holly AI. 이동평균 교차 전략의 기관 행동 기반 변형.
>
> **기대 시나리오**: 5일 이평 ↗ 50일 이평 교차 + 당일 2% 상승 → 3~10일간 5~8% 상승

### 전략 18: Alpha Predators [등급 B+] [HYBRID]
> **한줄 설명**: 기관이 조용히 매집하고 있는 종목을 포착 — 거래량은 높은데 가격 변동은 적다? 누군가 쌓고 있다.
>
> **원리**: 기관이 대량 매수할 때는 가격을 올리지 않으려고 조심스럽게 산다. 결과: 거래량은 평소보다 높지만, 가격은 좁은 범위에서 움직인다. 이 "조용한 매집" 패턴을 감지해서, 대중이 눈치채기 전에 먼저 올라타는 것.
>
> **출처**: Trade Ideas Holly AI. O'Neil CAN SLIM의 I(Institutional) 필터와 유사.
>
> **기대 시나리오**: 5일 거래량 증가 + 5일 레인지 축소 + 외국인/기관 3일 순매수 → 풀백 반등 시 진입 → 2~5일간 4% 상승

### 전략 19: Bullish Pullback [등급 A-] [HYBRID]
> **한줄 설명**: 잘 나가던 주식이 20~30% 빠졌을 때, 반등이 확인되면 → 다시 오를 거라고 베팅.
>
> **원리**: 상승 추세(60일 최고가 > 120일 이평)였던 종목이 일시적으로 풀백. 25% 풀백은 "할인 세일"이지만, 싸다고 바로 사면 안 되고 "반등 확인(3일 양봉 or 5일 이평 돌파)"을 기다려야 한다. 바닥을 잡으려 하지 말고, 바닥을 확인한 후 타기.
>
> **출처**: Trade Ideas Holly AI. Minervini의 "Buy at the right time" 원칙.
>
> **기대 시나리오**: 60일 고가 대비 25% 하락 → 3일 양봉(반등 확인) → 3~10일간 8~12% 상승

### 전략 20: Strong Stock Pulling Back [등급 B] [HYBRID]
> **한줄 설명**: Bullish Pullback의 저가 버전 — 시장이 약해도 상대적으로 강한 주식이 풀백할 때.
>
> **원리**: 시장 전체가 빠지는데 이 종목만 덜 빠진다? 그건 강한 종목이라는 증거. 이런 "상대 강세" 종목이 풀백하면, 시장이 반등할 때 가장 먼저, 가장 강하게 오른다.
>
> **출처**: Trade Ideas Holly AI.
>
> **기대 시나리오**: 시장 대비 상대 강세 유지 + 25% 풀백 → 반등 확인 → 2~5일간 5~8% 상승

### 전략 21: Quarterback [등급 A-] [HYBRID]
> **한줄 설명**: 큰 상승 후 25% 풀백 + RSI가 "과매도 탈출 중" → 정교한 풀백 매수.
>
> **원리**: 단순 풀백 매수에 RSI 이중 필터를 추가해서 정밀도를 높인 버전. RSI(14)가 30~50 사이(과매도 탈출 중이지만 과매수는 아님) + RSI(5) > RSI(14)(단기 모멘텀이 장기를 추월 = 가속 전환). 이 두 RSI 조건이 동시에 맞으면 "지금이 풀백 저점"일 확률이 높다.
>
> **출처**: Trade Ideas Holly AI. RSI 필터는 AI가 백테스트로 추가한 것.
>
> **기대 시나리오**: 20일 내 급등 후 25% 풀백 + RSI 교차 → 3~7일간 5~10% 반등

### 전략 22: Tailwind [등급 A-] [HYBRID]
> **한줄 설명**: 이평이 완전 정배열인 종목이 20일 이평에서 반등 — 순풍을 타고 계속 올라간다.
>
> **원리**: 5일 > 20일 > 60일 이평 정배열 = "순풍(tailwind)"이 불고 있는 상태. 이때 풀백이 20일 이평까지만 내려오고 반등하면, 이평이 "바닥"을 받쳐주는 것. 이평 아래로 안 내려간다 = 추세가 여전히 살아있다.
>
> **출처**: Trade Ideas Holly AI + Minervini SEPA 원리와 유사.
>
> **기대 시나리오**: 이평 정배열 + 20일 이평 터치 반등 → 2~5일간 4~6% 상승

### 전략 23: Trend Play [등급 A] [HYBRID]
> **한줄 설명**: 이평 리본(5/10/20/50/100/200일)이 전부 완벽하게 정배열 — 모든 시간축이 상승에 동의한다.
>
> **원리**: 6개 이평이 전부 순서대로 정렬되고 전부 상승 중이면, 단기·중기·장기 투자자 모두가 수익 중이고, 모두가 "더 올라갈 것"에 동의하는 상태. 이런 종목은 장기 투자자까지 보유를 유지하기 때문에 매도 압력이 약해서 상승이 지속된다.
>
> **출처**: Trade Ideas Holly AI. AQR 멀티팩터 모델과 유사한 "다중 팩터 정렬" 원리.
>
> **기대 시나리오**: 6단계 이평 완전 정배열 → 5분이평 반등 시 진입 → 5~20일간 5~10% 상승

### 전략 24: The Continuation [등급 B+] [HYBRID]
> **한줄 설명**: 2일 연속 양봉 + 오늘도 레인지 확장 중 = "계속 간다(continuation)."
>
> **원리**: 2일 연속 양봉 = 매수 모멘텀이 하루가 아니라 이틀째 유지. 거기에 오늘 레인지까지 평소보다 넓으면 "모멘텀이 가속 중". 30분 고가를 갱신하면 오늘도 계속 올라갈 확률이 높다.
>
> **출처**: Trade Ideas Holly AI.
>
> **기대 시나리오**: 전전일 양봉 + 전일 양봉 + 당일 레인지 확장 → 1~3일간 4% 추가 상승

### 전략 25: Got Dough Wants to Go [등급 B] [HYBRID]
> **한줄 설명**: 대형주(시총 1조+)가 모멘텀을 보이면 — "돈 있는 놈이 가려고 한다."
>
> **원리**: 대형주는 소형주와 달리 개인 투자자 몇 명으로는 안 움직여. 대형주가 일봉+장중 모두 모멘텀을 보이면 = 기관/외국인 대규모 매수가 들어오고 있다는 뜻. S&P(KOSPI) 양봉이 확인되면 시장 전체 센티먼트도 강세.
>
> **출처**: Trade Ideas Holly AI.
>
> **기대 시나리오**: 대형주 + 이평 상승 + 지수 양봉 + 외국인 당일 순매수 → 1~5일간 2~3% 상승

### 전략 26: The Guiding Hand [등급 B] [HYBRID]
> **한줄 설명**: 갭업 후 횡보 → 이평이 가격을 "안내하듯" 밀어올려서 추가 상승.
>
> **원리**: 갭업 = 관심 폭발. 근데 바로 더 오르지 않고 횡보(베이스)한다? 그건 이익실현 매물을 소화하고 있는 것. 이때 5분 이평(20)이 가격까지 따라잡아 올라와서 가격을 밀어올리면 = 다음 상승 준비 완료. "안내하는 손(guiding hand)" = 이평선.
>
> **출처**: Trade Ideas Holly AI.
>
> **기대 시나리오**: 갭업 2%+ → 1.5시간 횡보 → 이평 따라잡기 → 횡보 상단 돌파 → 3~5% 추가 상승

### 전략 27: Nice Chart [등급 A-] [HYBRID]
> **한줄 설명**: 전일 고가 돌파 + 이평 정배열 + RSI 적당 + 시장 강세 + 거래량 증가 = "차트가 예쁘다."
>
> **원리**: 여러 기술적 조건이 동시에 맞아떨어지는 종목은 드물다. 그래서 이 조건들이 전부 충족되면 "모든 게 맞아떨어진 상태" = 높은 확률의 매수 기회. 단일 조건이 아니라 다중 필터의 교집합이라 시그널 수는 적지만 승률이 높다.
>
> **출처**: Trade Ideas Holly AI.
>
> **기대 시나리오**: 6개 조건 동시 충족 → 2~5일간 4~6% 상승

### 전략 28: The Vault [등급 B] [HYBRID]
> **한줄 설명**: 10일 레인지 바닥에 갇혀 있던(금고=vault) 종목이 전일 고가를 돌파하며 탈출.
>
> **원리**: 10일 레인지 하위 30%에 있다 = 최근 10일간 약세. 근데 갑자기 전일 고가를 돌파하면서 RSI도 반등 시작 + 5일 이평 방향 전환 = "바닥에서 탈출하기 시작". 금고에서 나오는 종목은 눌려있던 에너지가 방출된다.
>
> **출처**: Trade Ideas Holly AI.
>
> **기대 시나리오**: 10일 바닥권 → 전일 고가 돌파 + RSI 반등 → 2~5일간 4% 상승

### 전략 29: Pulling The Arrow Back [등급 B+] [HYBRID]
> **한줄 설명**: 전일 장 전반에 하락했지만 장 후반에 강하게 반등 마감 → 다음날 양봉이면 "화살이 날아간다."
>
> **원리**: 활의 시위를 당기면(하락) 에너지가 축적되고, 놓으면(반등) 화살이 앞으로 날아간다. 전일 매도세가 장 후반에 실패했다는 건 "매도 총알이 떨어졌다"는 뜻. 다음날 양봉으로 시작하면 추세 전환 확인.
>
> **출처**: Trade Ideas Holly AI.
>
> **기대 시나리오**: 전일 전반 -2% 하락 → 후반 +2% 반등 → 다음날 양봉 확인 → 1~3일간 3% 상승

### 전략 30: Balloon Under Water [등급 B] [HYBRID]
> **한줄 설명**: 물속에 억지로 눌린 풍선은 결국 떠오른다 — 극단적 과매도(Z≤-3.0) 후 수급 전환 시 반등.
>
> **원리**: Z-Score -3.0 이하 = 통계적으로 매우 드문 수준의 과매도. 여기에 외국인 순매도가 종료(3일 중 1일 순매수 전환)되면, 가격을 누르던 힘이 빠지면서 "적정 가치"로 복귀. Revision to the Mean보다 더 극단적인 과매도에서만 진입하므로 시그널이 드물지만 승률이 높다.
>
> **출처**: Trade Ideas Holly AI + 르네상스 Statistical Arbitrage 원리.
>
> **기대 시나리오**: Z-Score -3.0 이하 + 외국인 순매도 종료 → 3~7일간 6~10% 반등

### 전략 31: NEO Breakout Long [등급 B-] [HYBRID]
> **한줄 설명**: 뉴스에 나온 종목이 거래량 3배+ 폭발 → 추세 방향으로 초기 진입.
>
> **원리**: Holly NEO는 "뉴스에 나온 고거래량 종목"만 감시. 뉴스 촉매(실적, 계약, M&A) + 거래량 폭발 = 시장의 관심이 집중된 상태. 이때 추세 방향(상승 중)으로 진입하면 관심이 지속되는 동안 수익. 한국에서는 텔레그램/네이버/DART로 뉴스 감지.
>
> **출처**: Trade Ideas Holly NEO.
>
> **기대 시나리오**: 뉴스 촉매 + 거래량 3배 → 전일 고가 돌파 → 1~3일간 5~10% 상승. 테마주 필터 필수.

### 전략 32: NEO Pullback Long [등급 B] [HYBRID]
> **한줄 설명**: NEO 급등 후 2~3일 횡보(숨 고르기) → 다시 돌파하면 "2차 상승" 시작.
>
> **원리**: 뉴스로 급등한 종목이 바로 더 가는 경우도 있지만, 2~3일 쉬었다 가는 경우가 더 많다. 횡보 = 이익실현 소화. 횡보 레인지 상단을 돌파하면 "소화 완료, 2차 상승 시작". 5일 이평이 가격을 따라잡으면 추가 확인.
>
> **출처**: Trade Ideas Holly NEO.
>
> **기대 시나리오**: NEO 급등 → 2~3일 횡보 → 횡보 상단 돌파 → 2~5일간 5% 추가 상승

---

# Section 4: 전략 상세 — Phase 3 (INTRADAY, 분봉 필수, 12개)

> 분봉 데이터 확보 후 구현.
> Phase 1~2 완료 후 추가.

### INTRADAY 전략 목록 + 설명

### 전략 33: 5 Day Bounce [등급 B] [INTRADAY]
> **한줄 설명**: 5일간 눌렸다가 저항을 뚫는 순간 = 매도세 소진 + 매수세 유입.
>
> **원리**: 5일 레인지 저점 근처에서 반등을 시도하면서 60분 고가를 갱신하고 저항선까지 돌파하면, 5일간 쌓인 매도 압력이 소진되었다는 뜻. 과매도 반전(Oversold Reversal) 전략.
>
> **출처**: Trade Ideas Holly AI.
>
> **기대 시나리오**: 5일 하락 → 60분 고가 갱신 + 저항 돌파 + 거래량 2배 → 반나절~2일간 3% 상승

### 전략 34: Breakout [등급 B] [INTRADAY]
> **한줄 설명**: 가장 기본적인 돌파 전략 — 저항선을 높은 거래량으로 뚫으면 매수.
>
> **원리**: 20일 고가(저항) 위로 가격이 올라가면, 그 위에서 매도하려고 기다렸던 물량이 소화된 것. 거래량이 높으면 "진짜 돌파", 낮으면 "거짓 돌파". 시장이 안정적(±0.5%)일 때 돌파가 더 의미 있다.
>
> **출처**: Trade Ideas Holly AI. 모든 돌파 전략의 기본형.
>
> **기대 시나리오**: 20일 고가 돌파 + 상대거래량 2배 + 시장 안정 → 반나절~3일간 3~5% 상승

### 전략 35: Early Bird [등급 B] [INTRADAY]
> **한줄 설명**: 장 초반 25분(09:05~09:30)에 조용한 돌파를 잡는다 — 아직 대중이 안 봤을 때.
>
> **원리**: 아침 장 시작 직후, 아직 거래량이 적고(당일 100만주 미만) 레인지가 좁은데 저항을 뚫는 종목 = 아직 시장의 관심을 못 받았지만 누군가 먼저 사고 있다. 10일·120분·15분 레인지가 모두 좁아야(에너지 축적) 진입.
>
> **출처**: Trade Ideas Holly AI.
>
> **기대 시나리오**: 장 초반 조용한 돌파 + 약간의 거래량 증가 → 당일~2일간 3% 상승

### 전략 36: Sunrise Mover [등급 B] [INTRADAY]
> **한줄 설명**: 장 시작 5~30분 사이에 전일 고가를 돌파하면서 이평도 정배열 → 오늘의 주인공.
>
> **원리**: 장 시작 직후 전일 고가를 돌파한다 = 어제보다 오늘 더 강하다. 여기에 이평(5일>20일) 상대강도까지 맞으면, 시장 전체가 양봉일 때 오늘 가장 먼저 움직이는 종목.
>
> **출처**: Trade Ideas Holly AI.
>
> **기대 시나리오**: 09:05~09:30 전일 고가 돌파 + 이평 정배열 → 반나절~2일간 3% 상승

### 전략 37: Pushing Through Resistance [등급 B] [INTRADAY]
> **한줄 설명**: 장 시작 후 20분 대기 → 오프닝 15분 레인지 상단 돌파 시 매수.
>
> **원리**: 장 시작 직후 15분(9:00~9:15)의 가격 범위 = "오프닝 레인지". 이 레인지는 전일 미국 시장, 야간 선물, 동시호가 결과가 반영된 "오늘의 기준선". 이걸 위로 뚫으면 "오늘은 올라갈 날". 40분 대기(한국 20분)로 초기 노이즈를 걸러냄.
>
> **출처**: Trade Ideas Holly AI. 오프닝 레인지 브레이크아웃(ORB) 전략의 변형.
>
> **기대 시나리오**: 9:20 이후 15분 레인지 고가 돌파 + 거래량 확인 → 반나절~2일간 레인지의 1.5배 상승

### 전략 38: Buyers Stepping [등급 B] [INTRADAY]
> **한줄 설명**: 당일 이미 2%+ 상승한 종목이 피보나치 38.2~50% 되돌림에서 반등 → 매수세가 "들어온다(stepping in)."
>
> **원리**: 피보나치 되돌림 = 기관 트레이더들이 보편적으로 사용하는 매수 가격대. 당일 크게 상승한 종목이 38.2~50% 수준까지 내려왔을 때 거래량이 증가하면 = 기관이 저 가격에서 매수하고 있다.
>
> **출처**: Trade Ideas Holly AI. 피보나치 되돌림 + 모멘텀 결합.
>
> **기대 시나리오**: 당일 2%+ 상승 → 피보나치 38.2~50% 풀백 → 반등 + 거래량 → 당일 고가 재돌파

### 전략 39: AVWAP Bounce [등급 B-] [INTRADAY]
> **한줄 설명**: 오늘과 어제의 VWAP(거래량 가중 평균가)에서 동시에 반등 → 기관 매수 가격대에서 지지.
>
> **원리**: VWAP = 그 날 거래된 모든 물량의 평균 단가. 기관들은 VWAP을 기준으로 "오늘 잘 샀는지" 평가하기 때문에, VWAP 근처에서 추가 매수하는 경향. 오늘 VWAP과 어제 VWAP이 비슷한 가격대이고 거기서 반등하면 = 이중 지지.
>
> **출처**: Trade Ideas Holly AI. 기관 트레이딩 벤치마크 활용.
>
> **기대 시나리오**: 현재가가 멀티데이 VWAP 터치 → 5분봉 3개 양봉 → 반나절~2일간 2~3% 상승

### 전략 40: Power Hour Long [등급 B] [INTRADAY]
> **한줄 설명**: 장 마감 전 마지막 50분(14:30~15:20) — 하루 종일 강했던 놈은 마감까지 강하다.
>
> **원리**: 장 후반은 기관/외국인의 마감 매매가 집중되는 시간. 이 시간에 전일 대비 강세 + 시장 대비 상대 강세 + VWAP 위에 있으면 = "기관이 마감 전에 추가 매수하고 있다". 오버나이트로 다음날 갭업까지 기대.
>
> **출처**: Trade Ideas Holly AI.
>
> **기대 시나리오**: 14:30~15:00 사이 조건 충족 → 다음날 시가 갭업 또는 진입가 대비 2% 상승

### 전략 41: Looking for a Bounce [등급 B] [INTRADAY]
> **한줄 설명**: 15분 오프닝 레인지를 아래로 이탈했지만, 지지선에서 반등 → 바닥 매수.
>
> **원리**: 오프닝 레인지 하향 이탈 = "오늘 하락할 수도 있다" 신호. 하지만 지지선(전일 저가, 20일 이평, 외국인 매수 가격대)에서 멈추고 반등하면 = "하락을 시도했지만 실패" = 오히려 매수 기회.
>
> **출처**: Trade Ideas Holly AI.
>
> **기대 시나리오**: 15분 레인지 하향 이탈 → 지지선 터치 → 5분봉 3개 양봉 → 반나절~1일간 2% 반등

### 전략 42: On Support [등급 B+] [INTRADAY]
> **한줄 설명**: Looking for a Bounce의 중대형주 버전 — 외국인/기관 매수 가격대가 최우선 지지선.
>
> **원리**: 중대형주(5만원+)는 외국인/기관의 매매 비중이 높아서, 이들의 평균 매수 가격대가 가장 강력한 지지선. 이 가격대에서 반등 + 호가 매수잔량 우위 = 기관이 물량을 받치고 있다.
>
> **출처**: Trade Ideas Holly AI. 한국 시장에서는 수급 기반 지지가 기술적 지지보다 우선.
>
> **기대 시나리오**: 외국인 매수 가격대 터치 → 반등 + 호가 매수 우위 → 반나절~2일간 2~3% 상승

### 전략 43: Revision To The Mean [등급 B+] [INTRADAY]
> **한줄 설명**: 장중 급락(Z≤-2.5)에서 모멘텀 전환이 확인되면 → "적정 가치로 돌아간다"에 베팅.
>
> **원리**: Z-Score -2.5 = 평소 변동 범위를 크게 벗어난 과매도. 통계적으로 극단값은 평균으로 회귀하는 경향이 있다. 단, "싸다고 바로 사면 안 되고" 모멘텀 전환(5분 RSI 반등, 3개 양봉)이 확인되어야 진입. "떨어지는 칼날"은 안 잡지만 "바닥에 떨어진 칼"은 줍는다.
>
> **출처**: Trade Ideas Holly AI + 르네상스 Statistical Arbitrage 원리.
>
> **기대 시나리오**: Z-Score -2.5 이하 → 모멘텀 전환 확인 → VWAP 또는 20일 이평까지 회귀 (반나절~2일)

### 전략 44: Separation From The 8 [등급 B+] [INTRADAY]
> **한줄 설명**: 10일 이평에서 5% 이상 아래로 벌어졌으면 → 이평으로 돌아올 때까지 베팅.
>
> **원리**: 이격도(separation) = 현재가와 이평의 거리. 너무 벌어지면 "고무줄 효과"로 이평 쪽으로 복귀하려는 힘이 작용. 10일 이평 대비 -5% 이상 이탈하면 과매도로 판단하고, 반등이 시작되면 진입. 목표가는 이평 복귀(이격도 0%).
>
> **출처**: Trade Ideas Holly AI. 한국에서는 8기간 → 10기간(2주)으로 수정.
>
> **기대 시나리오**: 10일 이평 대비 -5% 이하 → 반등 시작 → 3~5일간 이평까지 복귀 (5% 수익)

> 이 12개는 Phase 1~2의 EOD 대체 조건으로도 작동 가능.
> v3.0의 상세 조건 그대로 유지 + Section 0의 eod_alternative 적용.

---

# Section 5: 야간 전략 선정 시스템

```yaml
# config/strategy_selector.yaml
nightly_backtest:
  lookback_days: 20        # 최근 20거래일로 시뮬레이션
  metrics:
    - win_rate             # 승률
    - profit_factor        # 총이익/총손실
    - avg_rr_ratio         # 평균 R:R
    - signal_count         # 시그널 빈도
  min_win_rate: 0.40       # 승률 40% 미만 전략은 탈락
  min_profit_factor: 1.2   # 수익팩터 1.2 미만 전략은 탈락

market_regime:
  indicators:
    - kospi_trend: "KOSPI > 50일이평 → 상승, 아래 → 하락"
    - kosdaq_trend: "KOSDAQ > 50일이평 → 상승, 아래 → 하락"
    - volatility: "V-KOSPI200 또는 20일 실현변동성"
    - foreign_flow: "외국인 5일 순매수 방향"

  regime_strategy_mapping:
    상승장_저변동:
      weight_up: ["breakout", "gap_momentum", "legendary_trend"]
      weight_down: ["mean_reversion"]
    상승장_고변동:
      weight_up: ["pullback", "support_bounce"]
      weight_down: ["breakout"]  # 거짓 돌파 많아짐
    횡보장:
      weight_up: ["mean_reversion", "support_bounce"]
      weight_down: ["gap_momentum"]
    하락장:
      weight_up: []              # 모든 롱 전략 가중치 DOWN
      weight_down: ["all"]
      cash_weight: 0.5           # 최소 50% 현금

  max_active_strategies: 10      # 하루 최대 활성 전략 수
  min_active_strategies: 3       # 하루 최소 활성 전략 수

telegram_alerts:
  post_market: "내일 활성 전략 리스트 + 후보 종목"
  signal: "시그널 발생 시 즉시 알림 (종목/진입가/목표가/손절가)"
  daily_result: "장 마감 후 당일 결과 (전략/종목/수익률/승패)"
```

---

# Section 6: 코드 프로젝트 구조

```
quant-strategy-system/
├── config/
│   ├── universe.yaml
│   ├── market_filter.yaml
│   ├── supply_demand.yaml
│   ├── trading_cost.yaml
│   ├── risk.yaml
│   ├── strategy_selector.yaml
│   └── strategies/
│       ├── breakout.yaml
│       ├── pullback.yaml
│       ├── gap_momentum.yaml
│       ├── support_bounce.yaml
│       ├── mean_reversion.yaml
│       ├── neo.yaml
│       └── legendary.yaml
│
├── src/
│   ├── data/
│   │   ├── fetcher.py           # 데이터 수집 (pykrx, DART, etc.)
│   │   ├── preprocessor.py      # 로그수익률, 이평, RSI, 볼린저 등 계산
│   │   └── sector_mapper.py     # WICS 소분류 매핑 (wiseindex API)
│   │
│   ├── indicators/
│   │   ├── moving_averages.py   # 이동평균 (5/10/20/50/60/100/150/200)
│   │   ├── rsi.py               # RSI (14일, 5일)
│   │   ├── volume.py            # 상대거래량, 거래량 이평
│   │   ├── bollinger.py         # 볼린저 밴드
│   │   ├── vwap.py              # VWAP 수동 계산
│   │   ├── atr.py               # ATR
│   │   ├── rs_rating.py         # RS Rating (3/6/9/12개월 가중)
│   │   └── candle_patterns.py   # 해머, 장악형, 갭 패턴
│   │
│   ├── strategies/
│   │   ├── base.py              # BaseStrategy 부모 클래스
│   │   ├── breakout/            # 12개 브레이크아웃 전략
│   │   ├── pullback/            # 8개 풀백 전략
│   │   ├── gap_momentum/        # 6개 갭/모멘텀 전략
│   │   ├── support_bounce/      # 6개 지지/반등 전략
│   │   ├── mean_reversion/      # 4개 평균 회귀 전략
│   │   ├── neo/                 # 2개 NEO 전략
│   │   └── legendary/           # 7개 전설적 전략
│   │
│   ├── filters/
│   │   ├── universe_filter.py   # 시총/거래대금/가격 필터
│   │   ├── theme_filter.py      # 테마주/작전주 제외
│   │   ├── market_filter.py     # KOSPI/KOSDAQ/환율 필터
│   │   ├── supply_demand.py     # 외국인/기관 수급 등급
│   │   ├── event_filter.py      # 실적시즌/배당/만기 필터
│   │   └── time_filter.py       # 시간대별 신뢰도
│   │
│   ├── signal/
│   │   ├── signal_model.py      # Signal 데이터 클래스 (통일 형식)
│   │   ├── generator.py         # 전략별 시그널 생성
│   │   ├── dedup.py             # 중복 시그널 처리
│   │   └── confidence.py        # 신뢰도 가중치 계산
│   │
│   ├── portfolio/
│   │   ├── position_sizer.py    # 포지션 사이징 (Kelly 변형)
│   │   ├── exit_manager.py      # 통일 청산 규칙
│   │   └── risk_manager.py      # 종목/섹터/전체 한도 관리
│   │
│   ├── selector/
│   │   ├── nightly_backtest.py  # 야간 전략 백테스트
│   │   ├── regime_detector.py   # 시장 레짐 판별
│   │   └── strategy_selector.py # 내일 활성 전략 선정
│   │
│   ├── output/
│   │   ├── formatter.py         # 터미널/텍스트 출력
│   │   ├── exporter.py          # CSV/Excel 저장
│   │   └── telegram_bot.py      # 텔레그램 알림
│   │
│   ├── backtest/
│   │   ├── engine.py            # 백테스트 엔진
│   │   ├── walk_forward.py      # Walk-forward validation
│   │   └── metrics.py           # 승률/샤프/MDD 등 평가지표
│   │
│   └── main.py                  # 메인 실행 (일일 루틴)
│
├── output/                      # 일별 결과
├── logs/                        # 실행 로그
├── tests/                       # 유닛 테스트
└── README.md
```

---

# Section 7: BaseStrategy 클래스 사양

```python
# src/strategies/base.py

from dataclasses import dataclass
from typing import List, Optional, Dict
from abc import ABC, abstractmethod
import pandas as pd

@dataclass
class Signal:
    strategy_name: str
    ticker: str
    ticker_name: str
    direction: str = "LONG"
    entry_price: float = 0.0
    target_price: float = 0.0
    stop_loss_price: float = 0.0
    target_pct: float = 0.0
    stop_loss_pct: float = 0.0
    rr_ratio: float = 0.0
    hold_days_min: int = 1
    hold_days_max: int = 5
    confidence: float = 0.5
    exec_timing: str = "EOD"
    category: str = ""
    grade: str = "B"
    signal_date: str = ""
    sector: str = ""
    supply_demand_grade: str = "B"

class BaseStrategy(ABC):
    def __init__(self, config: Dict):
        self.name = ""
        self.category = ""
        self.exec_timing = "EOD"
        self.grade = "B"
        self.config = config

    @abstractmethod
    def scan(self, df: pd.DataFrame, universe_info: Dict) -> List[Signal]:
        """일봉 데이터로 시그널 스캔. 모든 전략이 이 메서드를 구현."""
        pass

    @abstractmethod
    def check_exit(self, position: Dict, current_data: pd.Series) -> Optional[str]:
        """청산 조건 확인. 반환값: None(유지), 'stop_loss', 'target', 'time', 'condition'"""
        pass

    def calculate_confidence(self, base_confidence: float,
                             time_weight: float,
                             supply_demand_grade: str,
                             market_filter_pass: bool,
                             fx_trend: str) -> float:
        """신뢰도 가중치 종합 계산"""
        confidence = base_confidence * time_weight

        sd_multiplier = {"A": 1.2, "B": 1.0, "C": 0.7}
        confidence *= sd_multiplier.get(supply_demand_grade, 1.0)

        if not market_filter_pass:
            confidence *= 0.5

        if fx_trend == "weakening":
            confidence *= 0.85

        return min(confidence, 1.0)
```

---

# Section 8: 구현 로드맵

```
Phase 1 (즉시 구현 — 일봉 EOD 12개)
├── Week 1: 인프라
│   ├── config yaml 파일 전부 생성
│   ├── BaseStrategy 클래스 구현
│   ├── Signal 데이터 클래스 구현
│   ├── 데이터 수집 모듈 (pykrx)
│   ├── 지표 계산 모듈 (이평, RSI, 거래량 등)
│   └── 유니버스 필터 구현
│
├── Week 2: Phase 1 전략 12개 구현
│   ├── pushing_the_spring.py
│   ├── engulfing.py
│   ├── yesterday_hammer.py
│   ├── snap_back_long.py
│   ├── horseshoe_up.py
│   ├── volume_doesnt_lie.py
│   ├── minervini_trend_template.py
│   ├── darvas_box.py
│   ├── weinstein_stage_analysis.py
│   ├── greenblatt_magic_formula.py
│   ├── piotroski_fscore.py
│   └── livermore_pivot_point.py
│
├── Week 3: 필터 + 출력 + 백테스트
│   ├── 수급 필터 (외국인/기관)
│   ├── 시장 필터 (KOSPI/KOSDAQ)
│   ├── 테마주 필터
│   ├── 중복 시그널 처리
│   ├── 통일 청산 규칙
│   ├── CSV 출력 + 터미널 출력
│   └── 12개 전략 백테스트 (최근 2년)
│
└── Week 4: 검증 + 텔레그램
    ├── Walk-forward validation
    ├── 파라미터 최적화 (config 수치 조정)
    ├── 텔레그램 알림 연동
    └── Phase 1 라이브 시작

Phase 2 (HYBRID EOD 부분 20개 추가)
├── Week 5~6: 20개 전략 EOD 스캔 부분 구현
├── Week 7: 야간 전략 선정 시스템
└── Week 8: Phase 2 백테스트 + 라이브

Phase 3 (INTRADAY 12개 + 분봉)
├── Week 9~10: 분봉 데이터 연동 + 12개 전략
├── Week 11: 실시간 시그널 시스템
└── Week 12: 전체 시스템 통합 + 최적화
```
